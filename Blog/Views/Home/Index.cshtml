@model IEnumerable<Post>

	@{

	}
<h2>Blog Home Page!</h2>
<img src="~/content/static/1.jpeg" width="500" />
<div>
	<ul>
		@foreach(var post in Model)
		{
			<li>
				<a asp-controller="Home" asp-action="Post" asp-route-id="@post.Id">@post.Title</a>
			</li>
		}
	</ul>
</div>